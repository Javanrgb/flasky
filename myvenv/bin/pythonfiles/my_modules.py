"""print("Imported my_modules")

test = "Test String"


def find_index(to_search, target):
    #finds index of a value
    for i, value in enumerate(to_search):
        if value == target:
            return i

    return -1

import random

courses = ['Math', 'Art','Biology','CompSci', 'Physics']
random_course = random.choice(courses)
print(random_course)

import math
rads = math.sin(math.radians(78))
print(rads)
"""
import antigravity

